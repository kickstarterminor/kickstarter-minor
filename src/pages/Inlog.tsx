import React from "react";
import InlogLayout from "../layouts/InlogLayout";
import InlogModule from "../components/InlogModule";

const Inlog: React.FC = () => {
  return (
    <InlogLayout>
      <InlogModule />
    </InlogLayout>
  );
};

export default Inlog;
